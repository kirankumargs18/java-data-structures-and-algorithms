package com.kirangs;

public class TestMain {

	public static void main(String[] args) {

		// arrays have to be pre allocated (size is fixed)
		// Insertion in the middle and deletion in the middle is costly in arrays
		// Implementation of queue and dequeue is complex with arrays

		// Linked List
		// each node stores data and reference of the next node in the list
		// no pre-allocation of memory

		//Below code
//		Map<Integer, String> map = new HashMap<>();
//		Random random = new Random();
//		while (true) {
//			map.put(random.nextInt(), "OutOfMemory");
//		}
	}
}
